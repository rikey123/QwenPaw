# -*- coding: utf-8 -*-
"""
Ralph-Loop Contract Tests

Contract tests for ralph-loop module interface compliance.

These tests verify that the ralph-loop modules (state, prompts, handler,
runner, dispatch, cancel) conform to expected interfaces. When any
interface changes, these tests catch regressions across all consumers.

Run:
    pytest tests/contract/agents/ralph_loop/ -v
"""
